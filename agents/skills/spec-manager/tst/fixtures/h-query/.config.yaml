status:
  - name: pending
    description: not started
  - name: done
    description: finished
taxonomy:
  layers: [area, component, section]
  structure:
    interface:
      tui: [results, errors]
    agents:
      agent-files: [spec-curator]
