status:
  - name: pending
    description: The spec is not implemented yet.
  - name: done
    description: The spec is implemented and verified.
taxonomy:
  layers:
    - area
    - component
    - section
  structure:
    agents:
      agent-files:
        - spec-curator
    build:
      testing:
        - commit
    isolation:
      container:
        - access
        - persistence
        - setup
      script:
        - output
        - setup
    tooling:
      spec-manager:
        - specs
        - site
        - change
