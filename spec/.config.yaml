status:
  - pending
  - done
taxonomy:
  layers:
    - area
    - component
    - section
  structure:
    agents:
      agent-files:
        - spec-curator
    isolation:
      container:
        - access
        - persistence
        - setup
      script:
        - output
        - setup
    tooling:
      spec-manager:
        - specs
